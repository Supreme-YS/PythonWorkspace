a = [10, 20, 30]

a.insert(0, 500) # 인덱스 0자리에 500을 삽입

a.insert(len(a), 500) # a의 길이는 a 인덱스 + 1 이므로, 끝 값에 500 삽입, a.append(500과 같음)

a.insert(1, [500, 600]) # 인덱스 1에 [500, 600]이라는 값 1개 삽입, 중첩 리스트

a[1:1] = [500, 600] # 슬라이싱을 통한 값 삽입, 인덱스 1자리에 500, 600

print(a)

